export interface daily_img {
  issue: string;
  birth: string;
  death: string;
}

export interface weekly_img {
  music: string;
  movie: string;
}

export interface wiki {
  id: number;
  year: string;
  event: string;
}